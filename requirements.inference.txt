# Inference-only dependencies (used by Docker backend image)
# Intentionally lightweight — no MLflow, no Optuna, no DVC
numpy
pandas
xgboost
scikit-learn
joblib
fastapi
uvicorn
python-multipart
pyyaml